server:
  port: 8770

spring:
  application:
    name: demo-two
  datasource:
    url: jdbc:mysql://localhost:3306/dev
    username: root
    password:

mybatis:
  type-aliases-package: com.example.two.entity
  mapper-locations: classpath:mapper/*.xml

